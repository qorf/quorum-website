use Libraries.Compute.Statistics.DataFrame
DataFrame frame
frame:Load("data/Cats.csv")
//select the column to calculate the mean of 
text someText = "The mean of the selected column is "
output someText + frame:Mean()