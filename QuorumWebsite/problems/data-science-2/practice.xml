<palette showAction="false" showControl="false" showBasic="false" selectedPage="Default">
    <page name="data-science-2">
        <header>Action Blocks</header>
        <block>frame:AddSelectedColumns("bill_length_mm,bill_depth_mm,flipper_length_mm")</block>
        <block>frame:AddSelectedFactors("species")</block>
        <block>BarChart chart = frame:BarChart()</block>
        <block>BarChart chart = frame:BarChartByColumnMean()</block>
        <block>BarChart chart = frame:BarChartByColumnMinimum()</block>
        <block>BarChart chart = frame:BarChartByColumnMaximum()</block>
        <block>BoxPlot chart = frame:BoxPlot()</block>
        <block>Histogram chart = frame:Histogram()</block>
        <block>ScatterPlot chart = frame:ScatterPlot()</block>
        <block>chart:SetTitle("Penguins")</block>
        <block>chart:SetSubtitle("Bill Depth and Length by Species")</block>
        <block>chart:SetYAxisTitle("Millimeters")</block>
        <block>chart:SetXAxisTitle("Species")</block>
        <block>chart:SetLegendLocationToTop()</block>
        <block>chart:SetLegendLocationToBottom()</block>
        <block>chart:SetLegendLocationToLeft()</block>
        <block>chart:ShowLegend(false)</block>
        <block>chart:ShowPatterns(true)</block>
        <block>chart:FlipOrientation()</block>
    </page>
</palette>