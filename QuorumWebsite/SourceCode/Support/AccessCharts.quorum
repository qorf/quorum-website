use Libraries.Web.Page.Division
class AccessCharts is Division
    action Init()
        me:AddClassAttribute("data11y-access-chart")
        me:SetIdentifier("ac1")// access charts gets randomized by the javascript
    end
end
