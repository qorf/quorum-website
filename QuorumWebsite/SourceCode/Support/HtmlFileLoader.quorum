use Libraries.Web.Page.Division
class HtmlFileLoader is Division
    text loaded = ""
    action Generate returns text
        return loaded
    end
    action GetLoaded returns text
        return loaded
    end

    action SetLoaded(text loaded)
        me:loaded = loaded
    end

end