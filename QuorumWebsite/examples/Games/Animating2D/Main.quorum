use Libraries.Game.Game
use Libraries.Game.Graphics.Drawable
use Libraries.System.File

class Main is Game
    Drawable spaceship

    action Main
        StartGame()
    end

    action CreateGame
        //Load on website root
        File file
        file:SetWorkingDirectory("/") 
        file:SetPath("media/games/alien.png")
        spaceship:Load(file)

        //Add the spaceship
        Add(spaceship)
    end

    action Update(number seconds)
    end
end