<palette showAction="false" showControl="false" showBasic="false" selectedPage="Data Science">
    <page name="Data Science">
        <header>Change the Colors</header>
        <block>chart:SetColorPaletteToCalm()</block>
        <block>chart:SetColorPaletteToDisturbing()</block>
        <block>chart:SetColorPaletteToExciting()</block>
        <block>chart:SetColorPaletteToMagma()</block>
        <block>chart:SetColorPaletteToPlayful()</block>
        <block>chart:SetColorPaletteToSerious()</block>
    </page>
</palette>