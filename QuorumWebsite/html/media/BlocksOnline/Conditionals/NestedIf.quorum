integer testScore = 75
text grade = ""
if testScore > 90
	grade = "A"
else
	if testScore > 80
    		grade = "B"
	else
    		if testScore > 70
        			grade = "C"
    		else
        			grade = "D"
    		end
	end
end
output "Student's grade: " + grade